% Table configuration for better formatting

% Increase cell padding for better readability
\renewcommand{\arraystretch}{1.5}

% Add extra column spacing  
\setlength{\tabcolsep}{8pt}

% Replace booktabs rules with standard hlines
\renewcommand{\toprule}{\hline}
\renewcommand{\midrule}{\hline}
\renewcommand{\bottomrule}{\hline}

% Allow tables to use full text width
\setlength{\LTcapwidth}{\textwidth}
\setlength{\LTleft}{0pt}
\setlength{\LTright}{\fill}
